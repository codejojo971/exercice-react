
const log = x => {console.log(x)}
log(5);
log('jo');

const h20 = temperature =>
if(temperature )

const isMultiple 
// utilisation fr la recursivite
const factoriel = nb => {
    if(nb === 1) return 1
    let total= nb* factoriel(nb -1)
    return total
}
console.log(factoriel(5))

const fibo = nb => {
    if (nb === 0) return 0
    else if ( nb === 1) return 1
    else return fibo (nb - 1) + fibo(nb - 2)

}
log(fibo(10))